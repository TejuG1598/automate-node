# automate-node

## How to run this app?

- Run `npm i`
- Run `export DEBUG=info,debug` on mac (Add environment variable)
- Run `export NODE_ENV=development` on mac (Add environment variable)
- Run `node index.js`